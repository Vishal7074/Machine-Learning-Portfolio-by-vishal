## Multivariate Linear Regression (Data Generation, Visualization & Evaluation)

This notebook focuses on **Multivariate Linear Regression** using a **synthetic dataset**
generated with `make_regression` from `sklearn`.

The goal of this notebook is to **understand how linear regression behaves when multiple
input features are involved**, and how model performance is evaluated using standard metrics.

---

## 📌 Dataset Used
- Dataset is **synthetically generated** using:
  ```python
  make_regression()
